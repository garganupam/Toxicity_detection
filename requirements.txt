# ── Core (both generation + detection) ───────────────────────────────────
torch>=2.1.0
transformers>=4.36.0
numpy>=1.24
pyyaml>=6.0

# ── ToxiGAN generation ──────────────────────────────────────────────────
sentence-transformers>=2.2.0
nltk>=3.8
requests>=2.31

# ── Detection classifier ────────────────────────────────────────────────
scikit-learn>=1.3
pandas>=2.0
datasets>=2.14
pyarrow>=14.0          # for parquet split caching

# ── Optional (but recommended) ──────────────────────────────────────────
tqdm>=4.66             # progress bars in training
matplotlib>=3.7        # for plots
